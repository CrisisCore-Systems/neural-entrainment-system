# Frontend env (prefix with VITE_ so Vite exposes it to the client)
VITE_API_URL=http://localhost:3001

# Note:
# Database settings belong in backend/.env, not here.
# Remove any DB_* entries from this file to avoid leaking secrets to the browser.