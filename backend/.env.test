# Test Environment Configuration
# Used for E2E tests and integration tests

NODE_ENV=test
PORT=3002

# PostgreSQL test database
DATABASE_URL=postgresql://postgres:kay@127.0.0.1:5432/neural_entrainment_test
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=neural_entrainment_test
DB_USER=postgres
DB_PASSWORD=kay

# Redis (use in-memory for tests)
REDIS_URL=redis://127.0.0.1:6379
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
DISABLE_REDIS=true

# JWT (test credentials - safe to commit)
JWT_SECRET=test-secret-key-for-e2e-and-integration-tests-only
JWT_EXPIRES_IN=7d

# CORS for test frontend
CORS_ORIGIN=http://localhost:5173

# Test user credentials (matches seed-test.sql)
# Email: testuser@example.com
# Password: TestPass123!
TEST_USER_EMAIL=testuser@example.com
TEST_USER_PASSWORD=TestPass123!

# Gateway test user
TEST_GATEWAY_EMAIL=gatewayuser@example.com
TEST_GATEWAY_PASSWORD=TestPass123!

# Admin test user
TEST_ADMIN_EMAIL=admin@example.com
TEST_ADMIN_PASSWORD=TestPass123!
